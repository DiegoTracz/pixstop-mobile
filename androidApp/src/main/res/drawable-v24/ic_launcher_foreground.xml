<!--
    Ícone do app (foreground) — fallback vector.
    Os PNGs em mipmap-* contêm o texto "Pixstop" renderizado com Press Start 2P.
    Este vector é usado apenas como fallback em dispositivos que não usam PNGs.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Transparent — PNGs são usados na prática -->
    <path android:fillColor="#00000000" android:pathData="M0,0h108v108h-108z" />
</vector>